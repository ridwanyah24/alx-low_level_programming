#include "main.h"
#include <string.h>
#include <stdio.h>
/**
  * _strchr - locates a character in a string
  *@s: the string
  *@c: the character
  *Return: the pointer to the string
  */
char *_strchr(char *s, char c)
{
	strchr(s, c);
	return (s);
}
