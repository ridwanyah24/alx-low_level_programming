#include <stdlib.h>
#include "main.h"
#include <stdio.h>
/**
  * _atoi - converts a string into an integer not explicit
  *@s: the string to be converted
  *Return: always 0;
  */
int _atoi(char *s)
{
	return (atoi(s));
}
