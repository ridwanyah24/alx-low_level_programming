#include <unistd.h>
#include <ctype.h>
#include "main.h"
/**
  * _islower - checks if a letter is lowercase
  *@parameter: integer
  *
  *Return : retuns 1 if letter is lower else returns 0
  */
int _islower(int)
{
	if (islower(int))
	{
		return (1);
	} else 
	{
		return (0);
	}
}
